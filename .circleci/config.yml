# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

orbs:
  # The Node.js orb contains a set of prepackaged CircleCI configuration you can utilize
  # Orbs reduce the amount of configuration required for common tasks.
  # See the orb documentation here: https://circleci.com/developer/orbs/orb/circleci/node
  node: circleci/node@4.7

executors:
  # These next lines define a Docker executor: https://circleci.com/docs/2.0/executor-types/
    # You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # A list of available CircleCI Docker Convenience Images are available here: https://circleci.com/developer/images/image/cimg/node
  node:
    docker:
      - image: cimg/node:16.10

commands:
  prettier-format:
    steps:
      - run:
          name: Prettier Format Fix
          command: yarn run format:fix # Fix auto fixable Prettier formatting errors
  
  eslint-and-stylelint-fix:
    steps:
      - run:
          name: ESLint & Stylelint Fix
          command: yarn run lint:fix # Fix auto fixable linting errors (JS and CSS)

jobs:
  build:
    executor: node
    steps:
      - checkout # pulls source code to the working directory

      # The node orb's install-packages step will install the dependencies from a package.json.
      # The orb install-packages step will also automatically cache them for faster future runs.
      - node/install-packages:
          pkg-manager: yarn

  format:
    executor: node
    steps:
      - checkout
      - prettier-format
      
  lint:
    executor: node
    steps:
      - checkout
      - eslint-and-stylelint-fix
      
workflows:
  # Below is the definition of your workflow.
  # Inside the workflow, you provide the jobs you want to run, e.g this workflow runs the build-and-test job above.
  # CircleCI will run this workflow on every commit.
  # For more details on extending your workflow, see the configuration docs: https://circleci.com/docs/2.0/configuration-reference/#workflows
  format-and-lint:
    jobs:
      - build
      - format:
          requires: 
            - build
      - lint:
          requires: 
            - build
